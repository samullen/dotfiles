function td {
  command=$1

  case $command in
    add    ) cat >> ~/Dropbox/todos.txt;;
    ls     ) cat ~/Dropbox/todos.txt;;
    edit   ) vim ~/Dropbox/todos.txt;;
    search ) ag -i $2 ~/Dropbox/todos.txt;;
    *      ) echo -n "Unknown command";;
  esac
}
